# Backtest run

Run ID: `20260903-backtest-pv-reference-parity-yty-v5`. `result.json` is canonical full precision; `raw/` preserves machine-oriented tables; `review/` contains human/LLM-oriented summaries.

Validation: first-round PV parity review complete; canonical Monthly Returns pagination was regression-tested and browser-validated with 12 rows per page.
