# Optimization run

Run ID: `20260828-0001`. `result.json` is canonical full precision; `raw/` preserves decimal tables; `review/` presents percentage-point columns suffixed `_pct` while ratios remain unitless.
