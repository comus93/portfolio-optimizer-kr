# Backtest run

Run ID: `20260903-backtest-pv-reference-parity-yty-v6`. `result.json` is canonical full precision; `raw/` preserves machine-oriented tables; `review/` contains human/LLM-oriented summaries.

Presentation validation for this run includes the report-level asset display contract: aggregate portfolio target weight descending, then asset name and ticker ascending; fixed asset colors across charts; and adaptive Monthly Correlations width with horizontal scrolling only below the readable minimum width.
