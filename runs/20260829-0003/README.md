# Optimization run

Run ID: `20260829-0003`. `result.json` is canonical full precision; `raw/` preserves decimal tables; `review/` presents percentage-point columns suffixed `_pct` while ratios remain unitless.
