# Backtest run

Run ID: `20260903-backtest-pv-reference-parity-yty-v1`. `result.json` is canonical full precision; `raw/` preserves machine-oriented tables; `review/` contains human/LLM-oriented summaries.

PV parity input uses `Time Period: Year-to-Year`, Start Year `2020`, End Year `2026`; portfolio weights, annual rebalancing, benchmark, initial balance, and risk-free settings are unchanged from the prior parity run.
