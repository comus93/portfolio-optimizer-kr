# Optimization run

Run ID: `20260828-pv-maxretvol15`. `result.json` is canonical full precision; `raw/` preserves decimal tables; `review/` presents percentage-point columns suffixed `_pct` while ratios remain unitless.
