# Design

The chart continues to stack positive values from zero upward and negative values from zero downward. At the latest observation, the visible top-to-bottom order is: positive series in reverse stack insertion order, then negative series in stack insertion order, then zero/unavailable series. The legend uses that order while retaining the original asset-to-color mapping.
